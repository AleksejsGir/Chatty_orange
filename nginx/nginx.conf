# nginx/nginx.conf

user  nginx;
worker_processes  auto; # Автоматически определит количество воркеров

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024; # Количество одновременных подключений на воркер
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    # Подключаем конфигурации сайтов из папки conf.d
    # Все файлы .conf в этой папке будут автоматически загружены.
    include /etc/nginx/conf.d/*.conf;
}

# <!-- TODO: Рассмотреть добавление настроек gzip для сжатия статики. -->
# <!-- TODO: Настроить upstream-блок для Gunicorn с несколькими воркерами для балансировки, если нужно. -->