# .gitignore
venv/
*.pyc
__pycache__
.env
# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# --- Django ---
# Log files
*.log
# SQLite database (игнорируем на случай локальных тестов)
db.sqlite3
db.sqlite3-journal
# Файл для локальных настроек (если будете использовать)
# local_settings.py

# --- Статика и Медиа ---
# Папки для СТАТИКИ, СОБРАННОЙ для production (НЕ должны быть в Git)
# Раскомментируйте, если используете collectstatic
static_root/
staticfiles/

# Папка для ЗАГРУЖЕННЫХ пользователем МЕДИА-файлов (НЕ должна быть в Git)
media/

# !!! ВАЖНО: НЕ игнорируем файлы ВНУТРИ папки static/ (css, js, images) !!!
# Папка static/ сама по себе НЕ игнорируется,
# так как содержит исходные статические файлы проекта (стили, базовые изображения).

# --- Виртуальные окружения ---
# Игнорируем папки виртуальных окружений
.venv/
venv/
env/
ENV/

# --- Файл с секретами ---
# ВАЖНО: Игнорируем файл с переменными окружения
.env
.env.*
!.env.example # Оставляем возможность добавить файл-пример .env.example

# --- Тестирование ---
# Отчеты о покрытии и кэш тестов
.coverage
.coverage.*
.pytest_cache/
# htmlcov/ # Раскомментировать, если генерируете HTML отчеты о покрытии

# --- IDE ---
# PyCharm
.idea/
# VS Code (на всякий случай, если кто-то использует)
# .vscode/

# --- Операционная система ---
# macOS
.DS_Store
# Windows
Thumbs.db

# --- Docker ---
# Файл для локальных переопределений Docker Compose
# docker-compose.override.yml.idea/
